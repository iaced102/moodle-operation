apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: moodle-service
  name: moodle-service
spec:
  ports:
    - name: "http"
      port: 80
      targetPort: 80
    - name: "https"
      port: 443
      targetPort: 443
  selector:
    app: moodle
#   type: LoadBalancer
