apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nfs-client-provisioner
  name: nfs-client-provisioner
spec:
  template:
    spec:
      containers:
        - name: nfs-client-provisioner
          env:
            - name: NFS_SERVER
              value: 123.30.234.224
            - name: NFS_PATH
              value: /moodle
      volumes:
        - name: nfs-client-root
          nfs:
            server: 123.30.234.224
            path: /moodle
